import { Routes } from '@angular/router';
import { HomeComponent } from './main-window/components/home/home.component';

export const routes: Routes = [
  {
    path: '',
    component: HomeComponent
  }
];
